# FQGate 自动发布维护

这个仓库是 FQGate 正式发行的唯一所有者。私有源码仓库只负责把 Windows x64、macOS arm64 和 macOS x86_64 的正式产物汇总到同一个 GitHub Draft Release；是否公开、Gitee 镜像、签名升级文档和稳定通道切换都由本仓库的“正式发布 FQGate”工作流决定。

## 一次性设置

在仓库 Settings → Actions → General 中把 Workflow permissions 设为 `Read and write permissions`，并配置：

- Actions secret `GITEE_TOKEN`：只授予 `qicuo/fqgate-releases` 所需的发行与仓库内容写权限。
- Actions secret `FQGATE_UPDATE_RELEASE_SIGNING_V1_PRIVATE_KEY`：`update-release-signing-v1` 的 Ed25519 私钥，仅用于不可变的 Release 与 Stable 文档。
- Actions secret `FQGATE_UPDATE_FRESHNESS_SIGNING_V1_PRIVATE_KEY`：`update-freshness-signing-v1` 的独立 Ed25519 私钥，仅用于短期 Freshness 在线证明。
- Actions variable `FQGATE_UPDATE_RELEASE_SIGNING_V1_PUBLIC_KEY`：Release/Stable 公钥的 32 字节 Base64URL 无填充编码。
- Actions variable `FQGATE_UPDATE_FRESHNESS_SIGNING_V1_PUBLIC_KEY`：Freshness 公钥的 32 字节 Base64URL 无填充编码。

两把私钥必须不同，也不能与其他用途共用。私钥只放 Actions secrets，禁止提交到 Git、写入构建产物或打印到日志。公钥不是秘密，但必须与客户端编译时内置的对应公钥完全一致。

私有 `fqgate/FQGate` 仓库继续使用仓库级 secret `FQGATE_RELEASES_TOKEN` 创建 GitHub Draft Release。它只需访问 `fqgate/FQGate-releases`，不需要 Gitee 或签名私钥。

## 签名升级文档

三个文档都使用 `{keyId,signed,signature}` 信封；`signed` 是正文原始 JSON 字节的 Base64URL 无填充编码，Ed25519 直接签这些原始字节。

- `releases/<version>/release.json`：不可变，包含 GitHub 实际公开时间 `publishedAt`、固定 `expiresAt = publishedAt + 30 天`、双源下载地址和三个平台的升级 ZIP。
- `releases/<sequence>/stable.json`：不可变，由发布序号定位并绑定对应 Release 文档的 SHA-256。
- `releases/freshness.json`：客户端唯一入口，绑定不可变 Stable 文档的路径与 SHA-256；有效期 12 小时，每 4 小时刷新一次，`refreshSequence` 每次递增。

根 `releases/stable.json` 保持 1.x 客户端要求的扁平清单格式，是 1.x 自动升级到 2.0 的正式兼容入口。Windows 指向独立 EXE 并使用 `replaceExecutable`；macOS 指向 ZIP 并使用 `openPackage`。2.0 客户端不读取该文件，而是读取签名 Freshness 链。

## 正式发布顺序

1. 完整下载并校验 GitHub Draft 中的发布请求、三个平台元数据、全部资产和 SHA-256 文件。
2. 把同一组资产镜像到 Gitee 预发布版本，逐个重新下载并比较字节与 SHA-256。任何镜像失败都会在公开前停止。
3. 先公开 Gitee Release，再公开 GitHub Release。若 GitHub 公开失败，立即把 Gitee 恢复为预发布状态，避免留下单边正式 Release。
4. 以 GitHub 返回的真实 `published_at` 生成 Release 文档；依次在 Gitee 与 GitHub 部署并回读不可变 Release、版本化 Stable 文档。
5. 最后把根 `releases/stable.json`（1.x 入口）与 `releases/freshness.json`（2.0 入口）作为一个发布事务切换。两份入口都先写 Gitee、再写 GitHub；任一写入失败都会恢复两端的旧入口。

Gitee 是硬门禁，不是异步的尽力镜像。稳定通道入口只会指向两端都已部署并回读一致的不可变文档。

## 定时 Freshness

工作流按 UTC 每 4 小时运行一次，只读取并验签当前双源入口、递增 `refreshSequence`，然后用独立 Freshness 私钥生成新的 12 小时在线证明。它不会修改或重签已经发布的 Release、Stable，也不会延长版本自身的 30 天有效期。

若仓库尚未完成首个新合同发布，定时任务会返回 `not_initialized`，不会为旧的未签名清单制造伪 Freshness。

## 故障恢复

- Draft 通知失败：手工运行“正式发布 FQGate”，输入同一版本。
- 校验或 Gitee 镜像失败：修复 Draft/预发布资产后重跑；公开资产不会被覆盖或删除。
- Gitee 已公开而 GitHub 本轮公开失败：工作流会优先把 Gitee 恢复为预发布；恢复失败会明确使任务失败，需要先处理该发行状态再重跑。
- Release 已双源公开但入口尚未切换：使用同一版本重跑。实际 GitHub `published_at` 和不可变文档保持不变，流程会继续部署并切换入口。
- 任一稳定入口第二端写入失败：工作流会同时恢复两端旧的 1.x Stable 与 2.0 Freshness；修复通道权限或服务故障后重跑即可。
- 两端现有 Freshness、Stable 或 Release 字节不一致：流程拒绝猜测哪一端正确。先人工核对签名与发布记录，再恢复成完全相同的文档。

已公开 Release 的资产和已经进入签名链的不可变文档禁止覆盖或删除。日常发布不需要手工编辑任何升级清单，也不要在两台构建机之间复制正式包。
