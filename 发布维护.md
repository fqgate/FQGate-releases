# FQGate 自动发布维护

本仓库同时保存 FQGate 1.x 与 2.0 的正式发行，但两代产品使用独立发布器、独立元数据合同和独立稳定入口。不得在一个脚本中根据版本号切换两套协议，也不得让 2.0 工作流改写 1.x 稳定通道。

## 发行边界

### FQGate 1.x

- 工作流：`.github/workflows/finalize-release.yml`
- 触发事件：`fqgate-draft-ready`
- 发布器：`scripts/finalize_release.py`
- 稳定入口：`releases/stable.json`
- 版本清单：`releases/<version>.json`
- 平台合同：Windows `replaceExecutable`，macOS `openPackage`

这套流程继续服务 1.0.3 及后续 1.x 维护版本。现有 1.x 打包脚本、平台元数据和自动升级合同保持不变。

### FQGate 2.0

- 工作流：`.github/workflows/finalize-v2-release.yml`
- 触发事件：`fqgate-v2-draft-ready`
- 发布器：`scripts/finalize_v2_release.py`
- 稳定入口：`releases/v2/freshness.json`
- 不可变 Stable：`releases/v2/<sequence>/stable.json`
- 不可变 Release：`releases/v2/<version>/release.json`
- 平台合同：三个平台统一使用包含主程序与独立 updater 的 `replaceApplication` ZIP

V2 发布器拒绝 1.x 版本，也不会读写根 `releases/stable.json`。1.x 发布器不依赖 V2 签名密钥和 V2 目录。

## 一次性设置

在仓库 Settings → Actions → General 中把 Workflow permissions 设为 `Read and write permissions`。

1.x 沿用现有 GitHub Release 权限，不需要升级签名密钥。V2 额外配置：

- Actions secret `GITEE_TOKEN`：只授予 `qicuo/fqgate-releases` 所需的发行与仓库内容写权限。
- Actions secret `FQGATE_UPDATE_RELEASE_SIGNING_V1_PRIVATE_KEY`：只签署 V2 不可变 Release 与 Stable 文档。
- Actions secret `FQGATE_UPDATE_FRESHNESS_SIGNING_V1_PRIVATE_KEY`：只签署 V2 短期 Freshness 在线证明。
- Actions variable `FQGATE_UPDATE_RELEASE_SIGNING_V1_PUBLIC_KEY`：Release/Stable 公钥的 32 字节 Base64URL 无填充编码。
- Actions variable `FQGATE_UPDATE_FRESHNESS_SIGNING_V1_PUBLIC_KEY`：Freshness 公钥的 32 字节 Base64URL 无填充编码。

两把 V2 私钥必须不同，也不能与其他用途共用。私钥只放 Actions secrets，禁止提交到 Git、写入构建产物或打印到日志。公钥必须与 2.0 客户端编译时内置的对应公钥完全一致。

私有 `fqgate/FQGate` 仓库继续使用仓库级 secret `FQGATE_RELEASES_TOKEN` 创建 GitHub Draft Release。1.x 分支发送 `fqgate-draft-ready`；2.0 分支发送 `fqgate-v2-draft-ready`。

## 1.x 正式发布顺序

1. 下载并校验 GitHub Draft 中的发布请求、三个平台元数据、全部资产和 SHA-256 文件。
2. 提交未发布的 `releases/<version>.json`。
3. 公开 GitHub Release。
4. 用 GitHub 返回的实际发布时间完成版本清单，并最后切换根 `releases/stable.json`。
5. 现有 Gitee 同步链路在 GitHub 发布成功后镜像公开仓库、标签与发行资产。

这条流程不读取 `releases/v2/`，也不要求独立 updater 或 Ed25519 签名文档。

## 2.0 正式发布顺序

1. 完整下载并校验 GitHub Draft 中的 V2 发布请求、三个平台元数据、升级 ZIP 和 SHA-256 文件。
2. 把同一组资产镜像到 Gitee 预发布版本，逐个重新下载并比较字节与 SHA-256。
3. 先公开 Gitee Release，再公开 GitHub Release；若 GitHub 公开失败，恢复 Gitee 的预发布状态。
4. 以 GitHub 返回的真实 `published_at` 生成并签署 `releases/v2/<version>/release.json`。
5. 生成并签署 `releases/v2/<sequence>/stable.json`。
6. 最后事务切换 GitHub 与 Gitee 的 `releases/v2/freshness.json`。

V2 的 Gitee 镜像是公开前硬门禁。所有 V2 通道文件都位于 `releases/v2/`，任何步骤都不得写入 1.x 的根稳定入口。

## V2 定时 Freshness

V2 工作流按 UTC 每 4 小时运行一次，只读取并验签当前双源 V2 入口、递增 `refreshSequence`，然后用独立 Freshness 私钥生成新的 12 小时在线证明。它不会修改 1.x 文件，也不会修改或重签已经发布的 V2 Release、Stable 文档。

若 V2 尚未完成首个正式发布，定时任务返回 `not_initialized`。

## 故障恢复

- 1.x Draft 通知失败：手工运行“正式发布 FQGate”，输入同一 1.x 版本。
- V2 Draft 通知失败：手工运行“正式发布 FQGate 2.0”，输入同一 V2 版本。
- 1.x 发布失败：只检查旧平台元数据、GitHub Release 和根稳定清单，不要引入 V2 包合同。
- V2 校验或 Gitee 镜像失败：修复 Draft 或预发布资产后重跑；公开资产和不可变签名文档不得覆盖。
- V2 两端 Freshness、Stable 或 Release 字节不一致：拒绝自动猜测正确来源，先人工核对签名与发布记录。

日常发布不需要手工编辑升级清单，也不要在两台构建机之间复制正式包。
